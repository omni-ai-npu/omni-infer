apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: omni-prom
    access: proxy
    url: http://localhost:9090      # host 网络下 Grafana 直连本机 Prometheus
    isDefault: true
    editable: true
