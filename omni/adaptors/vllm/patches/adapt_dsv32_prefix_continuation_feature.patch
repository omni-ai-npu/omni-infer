diff --git a/vllm/entrypoints/chat_utils.py b/vllm/entrypoints/chat_utils.py
index ec1b327..01423ac 100644
--- a/vllm/entrypoints/chat_utils.py
+++ b/vllm/entrypoints/chat_utils.py
@@ -175,6 +175,9 @@ class ConversationMessage(TypedDict, total=False):
     tool_calls: Optional[Iterable[ChatCompletionMessageToolCallParam]]
     """The tool calls generated by the model, such as function calls."""
 
+    prefix: Optional[bool]
+    """The prefix continuation flag."""
+
 
 # Passed in by user
 ChatTemplateContentFormatOption = Literal["auto", "string", "openai"]
@@ -1141,6 +1144,9 @@ def _parse_chat_message_content(
         if "name" in message and isinstance(message["name"], str):
             result_msg["name"] = message["name"]
 
+        if "prefix" in message and isinstance(message["prefix"], bool):
+            result_msg["prefix"] = message["prefix"]
+
     return result
 
 
